'use strict';

const notes = require('../notes.js');

describe('testing valid input from CLI', () => {
  test('something here', () => {
    
  })
})

//demo notes:

/*
SPY: 
//creat a spy that internally tracks the js engine calls to this function
jest.spyOn(global.console, 'log');

describe('testing valid input from CLI', () => {
  it('console log doe snot get call with no commands given', () => {
    let testNote = new notes();
    expect(console.log).not.toHaveBeenCalled();
  })

  it('console log is called when a valid command is given, ()=> {

  })
})
*/